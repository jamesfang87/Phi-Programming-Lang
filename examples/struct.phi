fun println(const msg: i32) {}

struct RGB {
    r: i32;
    b: i32;
    g: i32;

    public fun compareRed(const this, const other: i32) -> i32 {
        println(this.r);
        //println(other);
        return this.r - other;
    }
}

fun main() {
    const color = RGB { r = 255, b = 255, g = 255};
    println(color.r);
    println(12);
    println(color.compareRed(7));

}
