struct Triangle {
    public l: i32;
    public w: i32;
}

enum Shape {
    Rectangle: (i32, i32),
    Circle: i32,
    Square: i32,
    Triangle: Triangle
}

fun area(const shape: Shape) {
    match shape {
        Rectangle: { println("rectangle"); }
        Circle: { println("circle");  }
        Square:  println("square"),
        Triangle:  println("triangle")
    };

    println("done!");
}

fun main() {
    const foo = (1, 2, 3);
}

