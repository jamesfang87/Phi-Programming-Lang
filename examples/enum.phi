struct Triangle {
    public l: i32;
    public w: i32;
}

enum Shape {
    Rectangle: (i32, i32),
    Circle: i32,
    Square: i32,
    Triangle: Triangle
}

fun main() {
    const foo = (1, 2, 3);
}
