fun main() {
    for i in 0..10 {
    }

    while a {
        if a + 4 {
            return a + 1;
        } else if a + 4 {
            return a + 2;
        } else {
            return a + 3;
        }
        return a + 4;
    }
}
