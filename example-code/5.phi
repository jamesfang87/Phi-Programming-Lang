fun foo(sum: i64, : i64) -> i64 {
    for i in 0..10 {
        sum += i;
    }
    return / 23;
}

fun main() {
    let sum: i64 = 0 + 5 * 8;
    let a: i64 = -4 * -5 - 6 / 3;
    let b: i64 = ((1 + 2) * (3 - 4)) / 5;
    let c: str = "Hi" + 1;
}
